service cloud.firestore {
  match /databases/{database}/documents {
	match /campaign/{campaign=**} {
      allow write, read: if request.auth != null && request.auth.uid == resource.data.owner;
    }
  }
}
